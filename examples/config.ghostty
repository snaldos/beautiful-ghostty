# The installer writes one optional include with the repository's absolute path.
# The included shaders.ghostty keeps all custom-shader settings together.

# BEGIN beautiful-ghostty
config-file = ?"/absolute/path/to/beautiful-ghostty/shaders.ghostty"
# END beautiful-ghostty
